{
  "name": "whisper-installation-backend",
  "version": "0.1.0",
  "private": true,
  "description": "Node backend for a Max/MSP + Processing whisper installation.",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node test/smoke-test.js"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
